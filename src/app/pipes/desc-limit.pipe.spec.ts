import { DescLimitPipe } from './desc-limit.pipe';

describe('DescLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new DescLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
